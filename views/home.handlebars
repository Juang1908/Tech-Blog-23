<h1>Welcome to the Blog Site!</h1>
{{#if posts.length}}
  {{#each posts}}
    <div class="post">
      <h2><a href="/post/{{id}}">{{title}}</a></h2>
      <p class="date">{{createdAt}}</p>
    </div>
  {{/each}}
{{else}}
  <p>There are no posts yet. Be the first to write one!</p>
{{/if}}
